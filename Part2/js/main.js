$(document).ready(function(){
    $('h2').hide();      // structure: $(selector).action #it will select tag from html then apply action on that tag
    $('.me').hide();     // a class can be called and action can be set on that classed element 
    $('#ami').hide();    // an id can be called with a # tag 
})